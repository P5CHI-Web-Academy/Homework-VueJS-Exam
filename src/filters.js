import Vue from 'vue'

const statusFilterClosed = (issue) => {
  
}

const statusFilterOpened = (issue) => {
  
}

Vue.filter('statusFilterClosed', statusFilterClosed)
Vue.filter('statusFilterOpened', statusFilterOpened)
